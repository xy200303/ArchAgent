# ArchAgent 环境配置示例
# 复制为 .env.local 后填写密钥；.env.local 优先于 .env，切勿提交真实密钥。

# -----------------------------------------------------------------------------
# 基础与对话（必填）
# -----------------------------------------------------------------------------
ARCH_AGENT_THEME=light

# 腾讯混元 / OpenAI-compatible 主接口。HY3_* 是推荐名称，程序兼容 OPENAI_* 旧名称。
HY3_API_KEY=
HY3_BASE_URL=https://tokenhub.tencentmaas.com/v1
HY3_CHAT_MODEL=hy3

# Hy3 主模型固定接收图片多模态输入；无需额外视觉模型或开关。
HY3_THINKING_ENABLED=false
# 可选：minimal、low、medium、high、xhigh；默认使用 high。
HY3_REASONING_EFFORT=high
# 对话请求超时，单位秒。
HY3_REQUEST_TIMEOUT_S=120
HY3_CONTEXT_WINDOW_TOKENS=256000
HY3_MAX_OUTPUT_TOKENS=16000

# -----------------------------------------------------------------------------
# TokenHub 混元生图（preview_design / isolate_reference_object）
# -----------------------------------------------------------------------------
# 使用上方 HY3_API_KEY；hy-image-v3.0 为高质量默认模型。
HY3_IMAGE_ENDPOINT=https://tokenhub.tencentmaas.com/v1/api/image/lite
HY3_IMAGE_MODEL=hy-image-v3.0
# TokenHub 生图请求超时，单位秒。
HY3_IMAGE_REQUEST_TIMEOUT_S=300

# -----------------------------------------------------------------------------
# TokenHub 混元 3D（确认后的重建工作流）
# -----------------------------------------------------------------------------
# 使用上方 HY3_API_KEY；默认重建使用供应商 LowPoly。
HY3_3D_SUBMIT_ENDPOINT=https://tokenhub.tencentmaas.com/v1/api/3d/submit
HY3_3D_QUERY_ENDPOINT=https://tokenhub.tencentmaas.com/v1/api/3d/query
HY3_3D_MODEL=hy-3d-3.0
# 此面数仅用于显式普通细节生成，范围 3000-150000。
HY3_3D_FACE_COUNT=50000
# 生3D提交/查询请求超时、轮询间隔和单任务最大等待时间，单位秒。
HY3_3D_SUBMIT_TIMEOUT_S=120
HY3_3D_POLL_INTERVAL_S=3
HY3_3D_JOB_TIMEOUT_S=900

# -----------------------------------------------------------------------------
# 高级执行与导出（默认安全关闭）
# -----------------------------------------------------------------------------
# 启用后才向 Agent 暴露 exec_external_script；脚本仍须声明用途和预期产物。
AGENT_EXEC_BASH_ENABLED=false
# exec_external_script 的最长执行时间，单位秒。
AGENT_SCRIPT_TIMEOUT_S=60
AGENT_AUTO_PDF_EXPORT=false
# Windows 上 LibreOffice 的可执行文件路径；自动 PDF 导出时才需要。
LIBREOFFICE_PATH=
